function characterSequence(textInput) {
  let text = textInput;

  for (i = 0; i < text.length; i++) {
    let letter = text[i];
    console.log(letter);
  }
}

characterSequence("ice cream");
